# Evening call summary — Day 4

**Pair:** Kidane Gebremedhin Gidey + Nurye Nigus.
**Date:** 2026-05-09.

Feedback on Kidane's explainer (paired vs. unpaired tests on Nurye's `ablation_results.json`): Nurye found the variance-decomposition argument — `var(p̂₂ − p̂₁) = var(p̂₂) + var(p̂₁) − 2·cov(p̂₂, p̂₁)`, and that ignoring the covariance is *conservative*, not anti-conservative — extremely useful, because it inverted her prior assumption that the unpaired test was over-claiming significance. The reframing of "the two tests are not disagreeing — they are running on different samples" was the load-bearing reframe for her. Nurye flagged that the §3 small-sample math (`b + c < 6` cannot reject at two-sided 0.05 by construction) was correct but went past too quickly for a reader who hadn't seen exact-binomial McNemar before; Kidane added a one-paragraph worked example with the four `b, c` configurations at `b + c = 2` and the resulting p-values, so the "p = 1.0 is not no-effect" claim is grounded in the actual arithmetic rather than asserted. Tweet 4 was confusing without the prior tweets' context — Kidane rewrote it to lead with "p = 1.0 means the test cannot detect anything, not that nothing happened" so it stands alone for a reader who never clicks through. Section 6's recommended language for `method.md` was adopted near-verbatim.

Feedback on Nurye's explainer (the train-vs-held-out gap question): Kidane found the section that named the **adaptive-data-analysis bias accumulation rate** as a function of dev-access count extremely useful — the rough rule "every selection event spends a fraction of the held-out's honesty proportional to log(n_configs_compared)" gave him a way to size the gap without holding out a fresh partition. The recommendation to adopt a **Thresholdout-style call-counter** on `eval_dev.py` (log every dev access; cap at a budget; refuse evaluations past the cap unless the result is within `tolerance` of the cached estimate) is now Kidane's planned next protocol change. Kidane also flagged that the recommendation to wait for V0.2 to carve a proper test set could land harder if it explicitly named the tipping point — at what dev-access count does the contamination become large enough that the V0.2 cost is worth paying *now* — and Nurye added a one-line callout naming the count threshold (~50 evaluations on N=50 dev pairs, per the Reusable Holdout sample-complexity bound). Tweet 5 had the standard error magnitude as ±5 percentage points but the body said ±5%; fixed.

Both pairs signed off on the post-revision artifacts. Both blog posts and tweet threads cleared the public-artifact quality bar.

— Confirmed by Nurye Nigus.
